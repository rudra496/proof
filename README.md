# GitHub Pages + Supabase Private Upload

This project is now fully static (GitHub Pages compatible) and uses Supabase Storage directly from the browser.

- No backend server
- No Node.js runtime required
- No Python runtime required
- Private bucket uploads only
- Signed URL downloads only

Configuration is inside `index.html` using Supabase JS v2.
